// Operators --> operators are those who perform operations b/w two or more operands..

// Arithmetic operators-->
    // + , - , /, * , % , **

    // '+' operator do both addition and concatination..
    // '**' this is exponantion operator which work for power
        // 2 ** 3 = 8  (2 ki power 3)


// Comparison Operator --> 
   // == , === , != , !== , >= , <= , > , <

    //  == does not check the type...
    //  === checks type also..
    //  != not equal, also not ckeck the type
    // !== not equal equal , checks type also


// Assignment Operator -->
    // = , += , -= , *= , /= , %=

    // = will assign value to a variable 
    // += means jo bhi value hai usme + kardo...
    // example for all -->
        let a = 20;
        a+=2
        a-=2
        a*=2
        a/=2
        a%=3

// Logical Operator -->
    // && , || , !
   
// Unary Operator --> ek value pr lagte hai
    // + , - , ! ,  ++ , -- , typeOf 
    
    //  + is used to conver  the string number to actual number 
         let u = "3"  /*(check on console) */
    // - is used to make it negative
         let n = "3"
    // ! is used to see the type whether it is truthy or falsy

    // ++ increase the value 
       // if ++ is before the number means first increase the valuse and then print but if it is after the value which means first print the number and then after increase the value

       let num = 10;
       ++num  /*PreIncrement*/
       num++  /*PostIncrement*/
       
    //   same as -- operator...


// Ternary Operator -->
    // ?: ,

   // ?:
   // statement ? "true condition" : "false condtion"
    12 > 13 ? console.log("yess") : console.log("nooo")

   
